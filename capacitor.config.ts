import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sawit.pro.test',
  appName: 'sawit-pro',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
